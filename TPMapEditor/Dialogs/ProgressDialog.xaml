<local:DialogWindow x:Class="TPMapEditor.Dialogs.ProgressDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TPMapEditor.Dialogs"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        mc:Ignorable="d"
        Title="ProgessDialog" Height="450" Width="800" ShowInTaskbar="False">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <DockPanel LastChildFill="True">
            <Label Margin="-5,0,0,0" Content="{Binding CurrentOperation}" DockPanel.Dock="Top"/>
            <!--<ProgressBar x:Name="ProgressBar" DockPanel.Dock="Top" IsIndeterminate="{Binding ProgressBarIndeterminate}" Height="20" Value="100"/>-->
            <ListBox x:Name="LogsListBox" Margin="0,5,0,0" ItemsSource="{Binding Logs}"/>
        </DockPanel>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,15,0,0" HorizontalAlignment="Right">
            <Button Content="Close" MinWidth="85" Click="SaveButton_Click" IsEnabled="{Binding CanClose}"/>
        </StackPanel>
    </Grid>
</local:DialogWindow>
