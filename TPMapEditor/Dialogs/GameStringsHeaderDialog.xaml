<local:DialogWindow x:Class="TPMapEditor.Dialogs.GameStringsHeadersDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:controls="clr-namespace:TPMapEditor.Controls"
        xmlns:local="clr-namespace:TPMapEditor.Dialogs"
        xmlns:data="clr-namespace:TPMapEditor.Data"
        mc:Ignorable="d"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        Height="450" Width="800">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TabControl>
            <TabItem Header="Team names">
                <controls:CollectionEditorControl Margin="3" ItemsSource="{Binding AppSettings.TPTeamNames}" Factory="{Binding Factory}" GridOnlyMode="True">
                    <controls:CollectionEditorControl.Columns>
                        <DataGridTemplateColumn Header="Header file" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <ComboBox ItemsSource="{x:Static data:GameHeadersFile.GameHeadersFilesList}" SelectedValue="{Binding Item.FileName}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </controls:CollectionEditorControl.Columns>
                </controls:CollectionEditorControl>
            </TabItem>
            <TabItem Header="Speech events">
                <controls:CollectionEditorControl Margin="3" ItemsSource="{Binding AppSettings.TPSpeechEvents}" Factory="{Binding Factory}" GridOnlyMode="True">
                    <controls:CollectionEditorControl.Columns>
                        <DataGridTemplateColumn Header="Header file" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <ComboBox ItemsSource="{x:Static data:GameHeadersFile.GameHeadersFilesList}" SelectedValue="{Binding Item.FileName}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </controls:CollectionEditorControl.Columns>
                </controls:CollectionEditorControl>
            </TabItem>
            <TabItem Header="Speaker names">
                <controls:CollectionEditorControl Margin="3" ItemsSource="{Binding AppSettings.TPSpeakerNames}" Factory="{Binding Factory}" GridOnlyMode="True">
                    <controls:CollectionEditorControl.Columns>
                        <DataGridTemplateColumn Header="Header file" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <ComboBox ItemsSource="{x:Static data:GameHeadersFile.GameHeadersFilesList}" SelectedValue="{Binding Item.FileName}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </controls:CollectionEditorControl.Columns>
                </controls:CollectionEditorControl>
            </TabItem>
            <TabItem Header="Ship names">
                <controls:CollectionEditorControl Margin="3" ItemsSource="{Binding AppSettings.TPShipNames}" Factory="{Binding Factory}" GridOnlyMode="True">
                    <controls:CollectionEditorControl.Columns>
                        <DataGridTemplateColumn Header="Header file" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <ComboBox ItemsSource="{x:Static data:GameHeadersFile.GameHeadersFilesList}" SelectedValue="{Binding Item.FileName}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </controls:CollectionEditorControl.Columns>
                </controls:CollectionEditorControl>
            </TabItem>
            <TabItem Header="In game messages">
                <controls:CollectionEditorControl Margin="3" ItemsSource="{Binding AppSettings.TPInGameMessages}" Factory="{Binding Factory}" GridOnlyMode="True">
                    <controls:CollectionEditorControl.Columns>
                        <DataGridTemplateColumn Header="Header file" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <ComboBox ItemsSource="{x:Static data:GameHeadersFile.GameHeadersFilesList}" SelectedValue="{Binding Item.FileName}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </controls:CollectionEditorControl.Columns>
                </controls:CollectionEditorControl>
            </TabItem>
            <TabItem Header="Objective tasks">
                <controls:CollectionEditorControl Margin="3" ItemsSource="{Binding AppSettings.TPObjectiveTasks}" Factory="{Binding Factory}" GridOnlyMode="True">
                    <controls:CollectionEditorControl.Columns>
                        <DataGridTemplateColumn Header="Header file" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <ComboBox ItemsSource="{x:Static data:GameHeadersFile.GameHeadersFilesList}" SelectedValue="{Binding Item.FileName}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </controls:CollectionEditorControl.Columns>
                </controls:CollectionEditorControl>
            </TabItem>
            <TabItem Header="Speech events journals">
                <controls:CollectionEditorControl Margin="3" ItemsSource="{Binding AppSettings.TPSpeechEventsJournals}" Factory="{Binding Factory}" GridOnlyMode="True">
                    <controls:CollectionEditorControl.Columns>
                        <DataGridTemplateColumn Header="Header file" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <ComboBox ItemsSource="{x:Static data:GameHeadersFile.GameHeadersFilesList}" SelectedValue="{Binding Item.FileName}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </controls:CollectionEditorControl.Columns>
                </controls:CollectionEditorControl>
            </TabItem>
            <TabItem Header="Map text items">
                <controls:CollectionEditorControl Margin="3" ItemsSource="{Binding AppSettings.TPMapTextItems}" Factory="{Binding Factory}" GridOnlyMode="True">
                    <controls:CollectionEditorControl.Columns>
                        <DataGridTemplateColumn Header="Header file" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <ComboBox ItemsSource="{x:Static data:GameHeadersFile.GameHeadersFilesList}" SelectedValue="{Binding Item.FileName}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </controls:CollectionEditorControl.Columns>
                </controls:CollectionEditorControl>
            </TabItem>
            <TabItem Header="World names">
                <controls:CollectionEditorControl Margin="3" ItemsSource="{Binding AppSettings.TPWorldNames}" Factory="{Binding Factory}" GridOnlyMode="True">
                    <controls:CollectionEditorControl.Columns>
                        <DataGridTemplateColumn Header="Header file" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <ComboBox ItemsSource="{x:Static data:GameHeadersFile.GameHeadersFilesList}" SelectedValue="{Binding Item.FileName}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </controls:CollectionEditorControl.Columns>
                </controls:CollectionEditorControl>
            </TabItem>
            <TabItem Header="World descriptions">
                <controls:CollectionEditorControl Margin="3" ItemsSource="{Binding AppSettings.TPWorldDescriptions}" Factory="{Binding Factory}" GridOnlyMode="True">
                    <controls:CollectionEditorControl.Columns>
                        <DataGridTemplateColumn Header="Header file" Width="*">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <ComboBox ItemsSource="{x:Static data:GameHeadersFile.GameHeadersFilesList}" SelectedValue="{Binding Item.FileName}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </controls:CollectionEditorControl.Columns>
                </controls:CollectionEditorControl>
            </TabItem>
        </TabControl>
        <StackPanel Margin="0,15,0,0" Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Close" MinWidth="85" IsDefault="True" Click="SaveButton_Click"/>
        </StackPanel>
    </Grid>
</local:DialogWindow>
