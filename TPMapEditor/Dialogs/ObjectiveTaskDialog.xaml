<local:DialogWindow x:Class="TPMapEditor.Dialogs.ObjectiveTaskDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TPMapEditor.Dialogs"
        xmlns:settings="clr-namespace:TPMapEditor.Settings"
        xmlns:data="clr-namespace:TPMapEditor.Data"
        mc:Ignorable="d"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        Title="ObjectiveTaskDialog" Height="450" Width="800">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0*" MinWidth="200"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition MinWidth="200"/>
            </Grid.ColumnDefinitions>
            <Grid Margin="0">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <DataGrid Margin="10" HeadersVisibility="Column" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True" ItemsSource="{Binding Map.ObjectiveTasks}" SelectedItem="{Binding SelectedObjectiveTask}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Binding="{Binding Name}" Header="Name" Width="*"/>
                        <DataGridTemplateColumn MinWidth="70">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Remove" Click="RemoveObjectiveTaskButton_Click"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
                <Button Margin="10" Grid.Row="1" MinWidth="85" Content="Add objective task" Command="{Binding AddObjectiveTaskCommand}"/>
            </Grid>
            <GridSplitter Grid.Column="1" Width="5" ShowsPreview="True" ResizeBehavior="PreviousAndNext"/>
            <ScrollViewer Grid.Column="2" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="10,0,0,0">
                    <Label Margin="-5,0,0,0" Content="Name"/>
                    <TextBox Text="{Binding SelectedObjectiveTask.Name, ValidatesOnExceptions=True, NotifyOnValidationError=True}"/>
                    <Label Margin="-5,5,0,0" Content="Task"/>
                    <ComboBox ItemsSource="{x:Static data:StringDictionnary.ObjectiveTasksDictionnary}" SelectedValue="{Binding SelectedObjectiveTask.TextStringId}" SelectedValuePath="Key" DisplayMemberPath="Value"/>
                    <Label Margin="-5,5,0,0" Content="Active"/>
                    <CheckBox Content="{Binding SelectedObjectiveTask.Active}" IsChecked="{Binding SelectedObjectiveTask.Active}"/>
                    <Label Margin="-5,5,0,0" Content="Completed"/>
                    <CheckBox Content="{Binding SelectedObjectiveTask.Completed}" IsChecked="{Binding SelectedObjectiveTask.Completed}"/>
                    <Label Margin="-5,5,0,0" Content="Failed"/>
                    <CheckBox Content="{Binding SelectedObjectiveTask.Failed}" IsChecked="{Binding SelectedObjectiveTask.Failed}"/>
                </StackPanel>
            </ScrollViewer>
        </Grid>
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,15,0,0" HorizontalAlignment="Right">
            <Button Content="Close" MinWidth="85" Click="SaveButton_Click"/>
        </StackPanel>
    </Grid>
</local:DialogWindow>
