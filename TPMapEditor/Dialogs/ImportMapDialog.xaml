<local:DialogWindow x:Class="TPMapEditor.Dialogs.ImportMapDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:c="clr-namespace:CalcBinding;assembly=CalcBinding"
        xmlns:local="clr-namespace:TPMapEditor.Dialogs"
        mc:Ignorable="d"
        Title="ImportMapDialog" Height="450" Width="450">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ScrollViewer VerticalScrollBarVisibility="Auto">
            <StackPanel>
                <Label Margin="0,5,5,5" Content="Choose what you want to import"/>
                <CheckBox Margin="5" Content="Map size" IsChecked="{Binding ImportMapSize}"/>
                <CheckBox Margin="5" Content="World info" IsChecked="{Binding ImportWorldInfo}"/>
                <CheckBox Margin="5" Content="Flags" IsChecked="{Binding ImportFlags}" IsEnabled="{c:Binding '!ImportWorldRules'}"/>
                <CheckBox Margin="5" Content="Groups" IsChecked="{Binding ImportGroups}" IsEnabled="{c:Binding '!ImportWorldRules'}"/>
                <CheckBox Margin="5" Content="In game teams" IsChecked="{Binding ImportInGameTeams}" IsEnabled="{c:Binding '!ImportWorldRules'}"/>
                <CheckBox Margin="5" Content="Journal entries" IsChecked="{Binding ImportJournalEntries}"/>
                <CheckBox Margin="5" Content="Map text points" IsChecked="{Binding ImportMapTextPoints}" IsEnabled="{c:Binding '!ImportWorldRules'}"/>
                <CheckBox Margin="5" Content="Objective points" IsChecked="{Binding ImportObjectivePoints}" IsEnabled="{c:Binding '!ImportWorldRules'}"/>
                <CheckBox Margin="5" Content="Objective tasks" IsChecked="{Binding ImportObjectiveTasks}" IsEnabled="{c:Binding '!ImportWorldRules'}"/>
                <CheckBox Margin="5" Content="Player alliances" IsChecked="{Binding ImportPlayerAlliances}"/>
                <CheckBox Margin="5" Content="Players" IsChecked="{Binding ImportPlayers}" IsEnabled="{c:Binding '!ImportWorldRules and !ImportPlayerAlliances'}"/>
                <CheckBox Margin="5" Content="Selectable teams" IsChecked="{Binding ImportSelectableTeams}"/>
                <CheckBox Margin="5" Content="Speech events" IsChecked="{Binding ImportSpeechEvents}" IsEnabled="{c:Binding '!ImportWorldRules'}"/>
                <CheckBox Margin="5" Content="Timers" IsChecked="{Binding ImportTimers}" IsEnabled="{c:Binding '!ImportWorldRules'}"/>
                <CheckBox Margin="5" Content="Waypoint paths" IsChecked="{Binding ImportWaypointPaths}" IsEnabled="{c:Binding '!ImportWorldRules'}"/>
                <CheckBox Margin="5" Content="World arms" IsChecked="{Binding ImportWorldArms}"/>
                <CheckBox Margin="5" Content="World crews" IsChecked="{Binding ImportWorldCrews}"/>
                <CheckBox Margin="5" Content="World objects" IsChecked="{Binding ImportWorldObjects}" IsEnabled="{c:Binding '!ImportWorldRules and !ImportGroups'}"/>
                <CheckBox Margin="5" Content="World point sets" IsChecked="{Binding ImportWorldPointSets}" IsEnabled="{c:Binding '!ImportWorldRules'}"/>
                <CheckBox Margin="5" Content="World polygons" IsChecked="{Binding ImportWorldPolygons}" IsEnabled="{c:Binding '!ImportWorldRules'}"/>
                <CheckBox Margin="5" Content="World rules" IsChecked="{Binding ImportWorldRules}"/>
            </StackPanel>
        </ScrollViewer>
        <Grid Grid.Row="1" Margin="0,15,0,0" >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel Orientation="Horizontal">
                <Button Content="Check all" MinWidth="85" IsDefault="True" Command="{Binding CheckAllCommand}"/>
                <Button Content="Uncheck all" MinWidth="85" IsDefault="True" Command="{Binding UncheckAllCommand}" Margin="-1,0,0,0"/>
            </StackPanel>
            <Button Grid.Column="1" Content="Close" MinWidth="85" IsDefault="True" Click="SaveButton_Click" Margin="15,0,0,0"/>
        </Grid>
    </Grid>
</local:DialogWindow>
