<local:DialogWindow x:Class="TPMapEditor.Dialogs.WorldRuleDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:c="clr-namespace:CalcBinding;assembly=CalcBinding"
        xmlns:local="clr-namespace:TPMapEditor.Dialogs"
        xmlns:enums="clr-namespace:TPMapEditor.Enums"
        xmlns:converter="clr-namespace:TPMapEditor.Converter"
        xmlns:settings="clr-namespace:TPMapEditor.Settings"
        mc:Ignorable="d"
        DataContext="{Binding RelativeSource={RelativeSource Self}}"
        Title="WorldRuleDialog" Height="450" Width="800">
    <Window.Resources>
        <converter:EnumToDescriptionConverter x:Key="EnumDescriptionConverter"/>
    </Window.Resources>
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="0*" MinWidth="200"/>
                <ColumnDefinition Width="auto"/>
                <ColumnDefinition MinWidth="200"/>
            </Grid.ColumnDefinitions>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>
                <DataGrid Margin="10" HeadersVisibility="Column" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True" ItemsSource="{Binding Map.WorldRules}" SelectedItem="{Binding SelectedWorldRule}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Binding="{Binding Name}" Header="Name" Width="*"/>
                        <DataGridTemplateColumn MinWidth="70">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <Button Content="Remove" Click="RemoveWorldRule_Click"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>
                    </DataGrid.Columns>
                </DataGrid>
                <Button Margin="10" Grid.Row="3" MinWidth="85" Content="Add rule" Command="{Binding AddWorldRuleCommand}"/>
            </Grid>
            <GridSplitter Grid.Column="1" Width="5" ShowsPreview="True" ResizeBehavior="PreviousAndNext"/>
            <TabControl Grid.Column="2" Margin="10,0,0,0" Visibility="{c:Binding 'SelectedWorldRule!=null'}">
                <TabItem Header="Properties">
                    <StackPanel Margin="10">
                        <Label Margin="-5,0,0,0" Content="Name"/>
                        <TextBox Text="{Binding SelectedWorldRule.Name, UpdateSourceTrigger=LostFocus, ValidatesOnExceptions=True, NotifyOnValidationError=True}"/>
                        <CheckBox Margin="0,10" Content="Run once" IsChecked="{Binding SelectedWorldRule.RunOnce, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                </TabItem>
                <TabItem Header="Conditions">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" MinWidth="200"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition MinWidth="200"/>
                        </Grid.ColumnDefinitions>
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition/>
                                <RowDefinition Height="auto"/>
                            </Grid.RowDefinitions>
                            <DataGrid Margin="10" HeadersVisibility="Column" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True" ItemsSource="{Binding SelectedWorldRule.Conditions}" SelectedItem="{Binding SelectedRuleCondition}">
                                <DataGrid.Columns>
                                    <DataGridTextColumn  Binding="{Binding Type, Converter={StaticResource EnumDescriptionConverter}}" Header="Type" Width="*"/>
                                    <DataGridTemplateColumn MinWidth="70">
                                        <DataGridTemplateColumn.CellTemplate>
                                            <DataTemplate>
                                                <Button Content="Remove" Click="RemoveRuleCondition_Click"/>
                                            </DataTemplate>
                                        </DataGridTemplateColumn.CellTemplate>
                                    </DataGridTemplateColumn>
                                </DataGrid.Columns>
                            </DataGrid>
                            <Button Margin="10,10,10,7" Grid.Row="1" MinWidth="85" Content="Add condition" Command="{Binding AddRuleConditionCommand}"/>
                        </Grid>
                        <GridSplitter Grid.Column="1" Width="5" ShowsPreview="True" ResizeBehavior="PreviousAndNext"/>
                        <ScrollViewer Grid.Column="2" Visibility="{c:Binding 'SelectedRuleCondition!=null'}" VerticalScrollBarVisibility="Auto">
                            <StackPanel Grid.Column="2" Margin="10">
                                <Label Margin="-5,0,0,0" Content="Type"/>
                                <ComboBox ItemsSource="{enums:EnumBindingSource enums:RuleCondition}" SelectedValue="{Binding SelectedRuleCondition.Type, UpdateSourceTrigger=PropertyChanged}">
                                    <ComboBox.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Converter={StaticResource EnumDescriptionConverter}}"/>
                                        </DataTemplate>
                                    </ComboBox.ItemTemplate>
                                </ComboBox>
                                <StackPanel Visibility="{c:Binding 'SelectedRuleCondition.IsGroupUnitNameUnit!=null'}">
                                    <Label Margin="-5,5,0,0" Content="Group/Unit name"/>
                                    <ComboBox SelectedIndex="{Binding SelectedRuleCondition.IsGroupUnitNameUnit, UpdateSourceTrigger=PropertyChanged}">
                                        <ComboBoxItem Content="Group"/>
                                        <ComboBoxItem Content="Unit"/>
                                    </ComboBox>
                                    <ComboBox Margin="0,5,0,0" Visibility="{c:Binding '!SelectedRuleCondition.IsGroupUnitNameUnit'}" ItemsSource="{Binding Map.Groups}" SelectedValue="{Binding SelectedRuleCondition.GroupUnitName, UpdateSourceTrigger=PropertyChanged}" DisplayMemberPath="Name" SelectedValuePath="Name"/>
                                    <ComboBox Margin="0,5,0,0" Visibility="{c:Binding 'SelectedRuleCondition.IsGroupUnitNameUnit'}" ItemsSource="{Binding Map.ShipUnits}" SelectedValue="{Binding SelectedRuleCondition.GroupUnitName, UpdateSourceTrigger=PropertyChanged}" DisplayMemberPath="Name" SelectedValuePath="Name"/>
                                </StackPanel>
                                <StackPanel Visibility="{c:Binding 'SelectedRuleCondition.IsDockersNameUnit!=null'}">
                                    <Label Margin="-5,5,0,0" Content="Dockers name"/>
                                    <ComboBox SelectedIndex="{Binding SelectedRuleCondition.IsDockersNameUnit, UpdateSourceTrigger=PropertyChanged}">
                                        <ComboBoxItem Content="Group"/>
                                        <ComboBoxItem Content="Unit"/>
                                    </ComboBox>
                                    <ComboBox Margin="0,5,0,0" Visibility="{c:Binding '!SelectedRuleCondition.IsDockersNameUnit'}" ItemsSource="{Binding Map.Groups}" SelectedValue="{Binding SelectedRuleCondition.DockersName, UpdateSourceTrigger=PropertyChanged}" DisplayMemberPath="Name" SelectedValuePath="Name"/>
                                    <ComboBox Margin="0,5,0,0" Visibility="{c:Binding 'SelectedRuleCondition.IsDockersNameUnit'}" ItemsSource="{Binding Map.ShipUnits}" SelectedValue="{Binding SelectedRuleCondition.DockersName, UpdateSourceTrigger=PropertyChanged}" DisplayMemberPath="Name" SelectedValuePath="Name"/>
                                </StackPanel>
                                <StackPanel Visibility="{c:Binding 'SelectedRuleCondition.IsTargetsNameUnit!=null'}">
                                    <Label Margin="-5,5,0,0" Content="Targets name"/>
                                    <ComboBox SelectedIndex="{Binding SelectedRuleCondition.IsTargetsNameUnit, UpdateSourceTrigger=PropertyChanged}">
                                        <ComboBoxItem Content="Group"/>
                                        <ComboBoxItem Content="Unit"/>
                                    </ComboBox>
                                    <ComboBox Margin="0,5,0,0" Visibility="{c:Binding '!SelectedRuleCondition.IsTargetsNameUnit'}" ItemsSource="{Binding Map.Groups}" SelectedValue="{Binding SelectedRuleCondition.TargetsName, UpdateSourceTrigger=PropertyChanged}" DisplayMemberPath="Name" SelectedValuePath="Name"/>
                                    <ComboBox Margin="0,5,0,0" Visibility="{c:Binding 'SelectedRuleCondition.IsTargetsNameUnit'}" ItemsSource="{Binding Map.ShipUnits}" SelectedValue="{Binding SelectedRuleCondition.TargetsName, UpdateSourceTrigger=PropertyChanged}" DisplayMemberPath="Name" SelectedValuePath="Name"/>
                                </StackPanel>
                                <StackPanel Visibility="{c:Binding 'SelectedRuleCondition.IsGroupANameUnit!=null'}">
                                    <Label Margin="-5,5,0,0" Content="Group A name"/>
                                    <ComboBox SelectedIndex="{Binding SelectedRuleCondition.IsGroupANameUnit, UpdateSourceTrigger=PropertyChanged}">
                                        <ComboBoxItem Content="Group"/>
                                        <ComboBoxItem Content="Unit"/>
                                    </ComboBox>
                                    <ComboBox Margin="0,5,0,0" Visibility="{c:Binding '!SelectedRuleCondition.IsGroupANameUnit'}" ItemsSource="{Binding Map.Groups}" SelectedValue="{Binding SelectedRuleCondition.GroupAName, UpdateSourceTrigger=PropertyChanged}" DisplayMemberPath="Name" SelectedValuePath="Name"/>
                                    <ComboBox Margin="0,5,0,0" Visibility="{c:Binding 'SelectedRuleCondition.IsGroupANameUnit'}" ItemsSource="{Binding Map.ShipUnits}" SelectedValue="{Binding SelectedRuleCondition.GroupAName, UpdateSourceTrigger=PropertyChanged}" DisplayMemberPath="Name" SelectedValuePath="Name"/>
                                </StackPanel>
                                <StackPanel Visibility="{c:Binding 'SelectedRuleCondition.IsGroupBNameUnit!=null'}">
                                    <Label Margin="-5,5,0,0" Content="Group B name"/>
                                    <ComboBox SelectedIndex="{Binding SelectedRuleCondition.IsGroupBNameUnit, UpdateSourceTrigger=PropertyChanged}">
                                        <ComboBoxItem Content="Group"/>
                                        <ComboBoxItem Content="Unit"/>
                                    </ComboBox>
                                    <ComboBox Margin="0,5,0,0" Visibility="{c:Binding '!SelectedRuleCondition.IsGroupBNameUnit'}" ItemsSource="{Binding Map.Groups}" SelectedValue="{Binding SelectedRuleCondition.GroupBName, UpdateSourceTrigger=PropertyChanged}" DisplayMemberPath="Name" SelectedValuePath="Name"/>
                                    <ComboBox Margin="0,5,0,0" Visibility="{c:Binding 'SelectedRuleCondition.IsGroupBNameUnit'}" ItemsSource="{Binding Map.ShipUnits}" SelectedValue="{Binding SelectedRuleCondition.GroupBName, UpdateSourceTrigger=PropertyChanged}" DisplayMemberPath="Name" SelectedValuePath="Name"/>
                                </StackPanel>
                                <StackPanel Visibility="{c:Binding 'SelectedRuleCondition.IsTowingGroupNameUnit!=null'}">
                                    <Label Margin="-5,5,0,0" Content="Towing group name"/>
                                    <ComboBox SelectedIndex="{Binding SelectedRuleCondition.IsTowingGroupNameUnit, UpdateSourceTrigger=PropertyChanged}">
                                        <ComboBoxItem Content="Group"/>
                                        <ComboBoxItem Content="Unit"/>
                                    </ComboBox>
                                    <ComboBox Margin="0,5,0,0" Visibility="{c:Binding '!SelectedRuleCondition.IsTowingGroupNameUnit'}" ItemsSource="{Binding Map.Groups}" SelectedValue="{Binding SelectedRuleCondition.TowingGroupName, UpdateSourceTrigger=PropertyChanged}" DisplayMemberPath="Name" SelectedValuePath="Name"/>
                                    <ComboBox Margin="0,5,0,0" Visibility="{c:Binding 'SelectedRuleCondition.IsTowingGroupNameUnit'}" ItemsSource="{Binding Map.ShipUnits}" SelectedValue="{Binding SelectedRuleCondition.TowingGroupName, UpdateSourceTrigger=PropertyChanged}" DisplayMemberPath="Name" SelectedValuePath="Name"/>
                                </StackPanel>
                                <StackPanel Visibility="{c:Binding 'SelectedRuleCondition.IsTargetToTowNameUnit!=null'}">
                                    <Label Margin="-5,5,0,0" Content="Target to tow name"/>
                                    <ComboBox SelectedIndex="{Binding SelectedRuleCondition.IsTargetToTowNameUnit, UpdateSourceTrigger=PropertyChanged}">
                                        <ComboBoxItem Content="Group"/>
                                        <ComboBoxItem Content="Unit"/>
                                    </ComboBox>
                                    <ComboBox Margin="0,5,0,0" Visibility="{c:Binding '!SelectedRuleCondition.IsTargetToTowNameUnit'}" ItemsSource="{Binding Map.Groups}" SelectedValue="{Binding SelectedRuleCondition.TargetToTowName, UpdateSourceTrigger=PropertyChanged}" DisplayMemberPath="Name" SelectedValuePath="Name"/>
                                    <ComboBox Margin="0,5,0,0" Visibility="{c:Binding 'SelectedRuleCondition.IsTargetToTowNameUnit'}" ItemsSource="{Binding Map.ShipUnits}" SelectedValue="{Binding SelectedRuleCondition.TargetToTowName, UpdateSourceTrigger=PropertyChanged}" DisplayMemberPath="Name" SelectedValuePath="Name"/>
                                </StackPanel>
                                <StackPanel Visibility="{c:Binding 'SelectedRuleCondition.TargetToTowName!=null'}">
                                    <Label Margin="-5,5,0,0" Content="Target to tow name"/>
                                    <ComboBox SelectedIndex="{Binding SelectedRuleCondition.IsTargetToTowNameUnit, UpdateSourceTrigger=PropertyChanged}">
                                        <ComboBoxItem Content="Group"/>
                                        <ComboBoxItem Content="Unit"/>
                                    </ComboBox>
                                    <ComboBox Margin="0,5,0,0" Visibility="{c:Binding '!SelectedRuleCondition.IsTargetToTowNameUnit'}" ItemsSource="{Binding Map.Groups}" SelectedValue="{Binding SelectedRuleCondition.TargetToTowName, UpdateSourceTrigger=PropertyChanged}" DisplayMemberPath="Name" SelectedValuePath="Name"/>
                                    <ComboBox Margin="0,5,0,0" Visibility="{c:Binding 'SelectedRuleCondition.IsTargetToTowNameUnit'}" ItemsSource="{Binding Map.ShipUnits}" SelectedValue="{Binding SelectedRuleCondition.TargetToTowName, UpdateSourceTrigger=PropertyChanged}" DisplayMemberPath="Name" SelectedValuePath="Name"/>
                                </StackPanel>
                                <StackPanel Visibility="{c:Binding 'SelectedRuleCondition.HasGroupName'}">
                                    <Label Margin="-5,5,0,0" Content="Group name"/>
                                    <ComboBox ItemsSource="{Binding Map.Groups}" SelectedValue="{Binding SelectedRuleCondition.GroupName, UpdateSourceTrigger=PropertyChanged}" DisplayMemberPath="Name" SelectedValuePath="Name"/>
                                </StackPanel>
                                <StackPanel Visibility="{c:Binding 'SelectedRuleCondition.HasVolumeName'}">
                                    <Label Margin="-5,5,0,0" Content="Volume name"/>
                                    <ComboBox ItemsSource="{Binding Map.WorldPolygons}" SelectedValue="{Binding SelectedRuleCondition.VolumeName, UpdateSourceTrigger=PropertyChanged}" DisplayMemberPath="Name" SelectedValuePath="Name"/>
                                </StackPanel>
                                <StackPanel Visibility="{c:Binding 'SelectedRuleCondition.HasPlayerName'}">
                                    <Label Margin="-5,5,0,0" Content="Player name"/>
                                    <ComboBox ItemsSource="{Binding Map.Players}" SelectedValue="{Binding SelectedRuleCondition.PlayerName, UpdateSourceTrigger=PropertyChanged}" DisplayMemberPath="Name" SelectedValuePath="Name"/>
                                </StackPanel>
                                <StackPanel Visibility="{c:Binding 'SelectedRuleCondition.HasPlayerNameA'}">
                                    <Label Margin="-5,5,0,0" Content="Player name"/>
                                    <ComboBox ItemsSource="{Binding Map.Players}" SelectedValue="{Binding SelectedRuleCondition.PlayerNameA, UpdateSourceTrigger=PropertyChanged}" DisplayMemberPath="Name" SelectedValuePath="Name"/>
                                </StackPanel>
                                <StackPanel Visibility="{c:Binding 'SelectedRuleCondition.HasPlayerNameB'}">
                                    <Label Margin="-5,5,0,0" Content="Player name"/>
                                    <ComboBox ItemsSource="{Binding Map.Players}" SelectedValue="{Binding SelectedRuleCondition.PlayerNameB, UpdateSourceTrigger=PropertyChanged}" DisplayMemberPath="Name" SelectedValuePath="Name"/>
                                </StackPanel>
                                <StackPanel Visibility="{c:Binding 'SelectedRuleCondition.HasFlagName'}">
                                    <Label Margin="-5,5,0,0" Content="Flag name"/>
                                    <ComboBox ItemsSource="{Binding Map.Flags}" SelectedValue="{Binding SelectedRuleCondition.FlagName, UpdateSourceTrigger=PropertyChanged}" DisplayMemberPath="Name" SelectedValuePath="Name"/>
                                </StackPanel>
                                <StackPanel Visibility="{c:Binding 'SelectedRuleCondition.HasEquivalence'}">
                                    <Label Margin="-5,5,0,0" Content="Equivalence"/>
                                    <ComboBox ItemsSource="{enums:EnumBindingSource enums:Equivalence}" SelectedValue="{Binding SelectedRuleCondition.Equivalence, UpdateSourceTrigger=PropertyChanged}"/>
                                </StackPanel>
                                <StackPanel Visibility="{c:Binding 'SelectedRuleCondition.HasVitalSection'}">
                                    <Label Margin="-5,5,0,0" Content="Vital section"/>
                                    <ComboBox ItemsSource="{enums:EnumBindingSource enums:VitalSection}" SelectedValue="{Binding SelectedRuleCondition.VitalSection, UpdateSourceTrigger=PropertyChanged}"/>
                                </StackPanel>
                                <StackPanel Visibility="{c:Binding 'SelectedRuleCondition.HasWorldObjectType'}">
                                    <Label Margin="-5,5,0,0" Content="World object type"/>
                                    <ComboBox ItemsSource="{enums:EnumBindingSource enums:WorldObjectType}" SelectedValue="{Binding SelectedRuleCondition.WorldObjectType, UpdateSourceTrigger=PropertyChanged}"/>
                                </StackPanel>
                                <StackPanel Visibility="{c:Binding 'SelectedRuleCondition.HasSpeechEventName'}">
                                    <Label Margin="-5,5,0,0" Content="Speech event name"/>
                                    <ComboBox ItemsSource="{Binding Map.SpeechEvents}" SelectedValue="{Binding SelectedRuleCondition.SpeechEventName, UpdateSourceTrigger=PropertyChanged}" DisplayMemberPath="Name" SelectedValuePath="Name"/>
                                </StackPanel>
                                <StackPanel Visibility="{c:Binding 'SelectedRuleCondition.HasTeamName'}">
                                    <Label Margin="-5,5,0,0" Content="Team name"/>
                                    <ComboBox ItemsSource="{Binding Map.Teams}" SelectedValue="{Binding SelectedRuleCondition.TeamName, UpdateSourceTrigger=PropertyChanged}" DisplayMemberPath="Name" SelectedValuePath="Name"/>
                                </StackPanel>
                                <StackPanel Visibility="{c:Binding 'SelectedRuleCondition.HasTimerName'}">
                                    <Label Margin="-5,5,0,0" Content="Timer name"/>
                                    <ComboBox ItemsSource="{Binding Map.Timers}" SelectedValue="{Binding SelectedRuleCondition.TimerName, UpdateSourceTrigger=PropertyChanged}" DisplayMemberPath="Name" SelectedValuePath="Name"/>
                                </StackPanel>
                                <StackPanel Visibility="{c:Binding 'SelectedRuleCondition.HasFlagType'}">
                                    <Label Margin="-5,5,0,0" Content="Flag texture"/>
                                    <ComboBox ItemsSource="{x:Static settings:AppSettings.FlagTextures}" SelectedValue="{Binding SelectedRuleCondition.FlagType, UpdateSourceTrigger=PropertyChanged}"/>
                                </StackPanel>
                                <StackPanel Visibility="{c:Binding 'SelectedRuleCondition.BoolValue!=null'}">
                                    <Label Margin="-5,5,0,0" Content="Value"/>
                                    <CheckBox IsChecked="{Binding SelectedRuleCondition.BoolValue}" Content="{Binding SelectedRuleCondition.BoolValue}"/>
                                </StackPanel>
                                <StackPanel Visibility="{c:Binding 'SelectedRuleCondition.Exists!=null'}">
                                    <Label Margin="-5,5,0,0" Content="Exists"/>
                                    <CheckBox IsChecked="{Binding SelectedRuleCondition.Exists}" Content="{Binding SelectedRuleCondition.Exists}"/>
                                </StackPanel>
                                <StackPanel Visibility="{c:Binding 'SelectedRuleCondition.EntireGroup!=null'}">
                                    <Label Margin="-5,5,0,0" Content="Entire group"/>
                                    <CheckBox IsChecked="{Binding SelectedRuleCondition.EntireGroup}" Content="{Binding SelectedRuleCondition.EntireGroup}"/>
                                </StackPanel>
                                <StackPanel Visibility="{c:Binding 'SelectedRuleCondition.Distance!=null'}">
                                    <Label Margin="-5,5,0,0" Content="Distance"/>
                                    <Slider Value="{Binding SelectedRuleCondition.Distance}" IsSnapToTickEnabled="True" SmallChange="1" Minimum="0" Maximum="9999"/>
                                </StackPanel>
                                <StackPanel Visibility="{c:Binding 'SelectedRuleCondition.NumberOfRoundsFired!=null'}">
                                    <Label Margin="-5,5,0,0" Content="Number of rounds fired"/>
                                    <Slider Value="{Binding SelectedRuleCondition.NumberOfRoundsFired}" IsSnapToTickEnabled="True" SmallChange="1" Minimum="0" Maximum="9999"/>
                                </StackPanel>
                                <StackPanel Visibility="{c:Binding 'SelectedRuleCondition.NumberOfTimesHit!=null'}">
                                    <Label Margin="-5,5,0,0" Content="Number of times hit"/>
                                    <Slider Value="{Binding SelectedRuleCondition.NumberOfTimesHit}" IsSnapToTickEnabled="True" SmallChange="1" Minimum="0" Maximum="9999"/>
                                </StackPanel>
                                <StackPanel Visibility="{c:Binding 'SelectedRuleCondition.NumberInGroup!=null'}">
                                    <Label Margin="-5,5,0,0" Content="Number in group"/>
                                    <Slider Value="{Binding SelectedRuleCondition.NumberInGroup}" IsSnapToTickEnabled="True" SmallChange="1" Minimum="0" Maximum="9999"/>
                                </StackPanel>
                                <StackPanel Visibility="{c:Binding 'SelectedRuleCondition.NumberOfCaptures!=null'}">
                                    <Label Margin="-5,5,0,0" Content="Number of captures"/>
                                    <Slider Value="{Binding SelectedRuleCondition.NumberOfCaptures}" IsSnapToTickEnabled="True" SmallChange="1" Minimum="0" Maximum="9999"/>
                                </StackPanel>
                                <StackPanel Visibility="{c:Binding 'SelectedRuleCondition.NumberOfShipsDestroyed!=null'}">
                                    <Label Margin="-5,5,0,0" Content="Number of ships destroyed"/>
                                    <Slider Value="{Binding SelectedRuleCondition.NumberOfShipsDestroyed}" IsSnapToTickEnabled="True" SmallChange="1" Minimum="0" Maximum="9999"/>
                                </StackPanel>
                                <StackPanel Visibility="{c:Binding 'SelectedRuleCondition.Points!=null'}">
                                    <Label Margin="-5,5,0,0" Content="Points"/>
                                    <Slider Value="{Binding SelectedRuleCondition.Points}" IsSnapToTickEnabled="True" SmallChange="1" Minimum="-9999" Maximum="9999"/>
                                </StackPanel>
                                <StackPanel Visibility="{c:Binding 'SelectedRuleCondition.TimeInSeconds!=null'}">
                                    <Label Margin="-5,5,0,0" Content="Time in seconds"/>
                                    <Slider Value="{Binding SelectedRuleCondition.TimeInSeconds}" IsSnapToTickEnabled="True" SmallChange="1" Minimum="0" Maximum="9999"/>
                                </StackPanel>
                                <StackPanel Visibility="{c:Binding 'SelectedRuleCondition.NumberOfObjectsThatHaveAlreadyEnteredWaitingToReport!=null'}">
                                    <Label Margin="-5,5,0,0" Content="Number of objects that have already entered waiting to report"/>
                                    <Slider Value="{Binding SelectedRuleCondition.NumberOfObjectsThatHaveAlreadyEnteredWaitingToReport}" IsSnapToTickEnabled="True" SmallChange="1" Minimum="0" Maximum="9999"/>
                                </StackPanel>
                                <StackPanel Visibility="{c:Binding 'SelectedRuleCondition.DamagePercent!=null'}">
                                    <Label Margin="-5,5,0,0" Content="Damage percent"/>
                                    <Slider Value="{Binding SelectedRuleCondition.DamagePercent}" IsSnapToTickEnabled="True" TickFrequency="0.001" LargeChange="0.001" SmallChange="0.001" Minimum="0" Maximum="1"/>
                                </StackPanel>
                            </StackPanel>
                        </ScrollViewer>
                    </Grid>
                </TabItem>
                <TabItem Header="Actions">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition Height="auto"/>
                        </Grid.RowDefinitions>
                        <DataGrid Margin="10" HeadersVisibility="Column" AutoGenerateColumns="False" CanUserAddRows="False" CanUserDeleteRows="False" IsReadOnly="True" ItemsSource="{Binding SelectedWorldRule.Actions}" SelectedItem="{Binding SelectedRuleAction}">
                            <DataGrid.Resources>
                                <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="Transparent"/>
                            </DataGrid.Resources>
                            <DataGrid.Columns>
                                <DataGridTextColumn Binding="{Binding Type, Converter={StaticResource EnumDescriptionConverter}}" Header="Action type" Width="*"/>
                                <DataGridTemplateColumn MinWidth="70">
                                    <DataGridTemplateColumn.CellTemplate>
                                        <DataTemplate>
                                            <Button Content="Remove" Click="RemoveRuleAction_Click"/>
                                        </DataTemplate>
                                    </DataGridTemplateColumn.CellTemplate>
                                </DataGridTemplateColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                        <Button Margin="10,10,10,7" Grid.Row="1" MinWidth="85" Content="Add action" Command="{Binding AddRuleActionCommand}"/>
                    </Grid>
                </TabItem>
            </TabControl>
        </Grid>
    </Grid>
</local:DialogWindow>
